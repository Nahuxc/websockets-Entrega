<div>
    <div class="content">
        {{#each prod}}
        <div class="div-1">
            <h1>producto ID: {{this.id}} </h1>
            <p>{{this.title}}</p>
            <p>{{this.description}} </p>
            <p>{{this.img}}</p>
            <p>{{this.price}}</p>
            <p>{{this.stock}}</p>
        </div>
        {{/each}}
    </div>
</div>
<script src="/socket.io/socket.io.js"></script>